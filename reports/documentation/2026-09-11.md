# Documentation and backend audit — 11 September 2026

Status: complete documentation audit; final artifacts are committed with this record.
One pre-existing offline cache-dependent test failure is retained below.
Source baseline: `a96025e`. Published v7 source remains `6efbbfd5a57cb557683e636d1fe20ae63b6b9eda`.

Scope: user-requested README/docs rewrite, scientific report of all AI/provider,
Council, numerical and crop-growth mechanisms, detailed game backend audit,
reproducible figures and specification corrections for future development.
Application runtime code, coefficients, releases and game records are unchanged.

Three bounded gpt-5.6-sol specialists audited AI/provider, numerical/growth and
system/data architecture; the architecture specialist subsequently audited full
game state machines. Root integrated docs/specifications and generated figures.
No recursive delegation or runtime provider inference was used for documentation.

## Verification results

- GitHub permission inspection: write/admin access; clean synchronized baseline.
- Fly read-only inspection: one started Singapore Machine, 1/1 health checks,
  4 shared vCPUs/4096 MB, one 3-GB encrypted volume; registry lists v1–v7.
- Root offline provider/contracts/model group: **166 passed**, 2 dependency
  deprecation warnings, **17.62 seconds**. Exact command is the first test group
  in [reproducibility](../../docs/technical/reproducibility.md).
- Specialist numerical/planner/research group: **35 passed**, **57.09 seconds**;
  command and scope in [numerical chapter](../../docs/technical/numerical-models-and-growth.md).
  This overlaps the root group's explorer-numerics tests; counts are not additive.
- Figure generation: three SVGs, three PNGs and fixture/forecast `data.json`;
  all PNGs visually inspected. No database/network/solver/provider call by generator.

The checkout initially had no `.venv` or installed application dependencies.
A temporary `/tmp/farmtact-docs-venv` was provisioned from `requirements.lock.txt`,
plus documentation-only Markdown/CairoSVG. Native Cairo and DejaVu fonts were
installed for raster rendering. Root application dependency files are unchanged.
The initial PNG attempt lacked native Cairo; installation resolved it.

No new authenticated model trial, public browser/capacity experiment, full
PostgreSQL suite, farm-state mutation or application deployment is claimed.
Historical reports retain their original evidence and known unsupported replies.

- Root game/research/conversation group: **66 passed, 1 failed, 1 deselected**,
  2 deprecation warnings, **36.06 seconds**. The PostgreSQL-specific test was
  deliberately deselected; all other tests used local fixtures/SQLite/mocks.
  Failure: `test_seven_current_advisors_and_frozen_create_idempotency`, expecting
  `source:D04.summary` despite absent ignored public cache. Source inspection
  confirms the conversation context reads `source_views()`. Recorded as DATA-06;
  no live fetch or weakened assertion was used to manufacture a pass.

```bash
/tmp/farmtact-docs-venv/bin/python -m pytest -q \
  tests/council_research/test_sessions.py \
  tests/council_research/test_independent_review.py \
  tests/gameplay/test_scenarios.py \
  tests/gameplay/test_conversations.py -k 'not real_postgres'
```

- Generated web-contract drift check passed.
- First documentation milestone committed/pushed as `9c72af8`; final chapters,
  rendered diagrams, specification integration and checks follow in a second commit.


## Final report artifacts and checks

- Seven-chapter [scientific report](../../docs/technical/README.md), including a
  dedicated 18-gap game/backend audit and the AI/numerical/data gap register.
- Three source-derived scientific figures (SVG + PNG), **20 rendered Mermaid
  diagrams** with source hashes, and two explicitly archived application screenshots.
- Self-contained [HTML](farmtact-technical-report.html) and [PDF](farmtact-technical-report.pdf)
  reading copies; numerical equations rendered by local MathJax.
- Browser export checks: **7 chapters, 25 loaded images, 53 equations**, no broken
  image/internal anchor, no horizontal page overflow and no external network request.
  Exact results: [render-checks.json](render-checks.json). The browser inspected the
  local documentation file only; this is not an application UI regression run.
- Documentation check: **26 Markdown files, 375 relative links and 20 diagram
  source hashes/SVGs**, zero errors. Python source line anchors were range-checked.
  Markdown source/fence and whitespace checks pass. External citation review is
  described in the provider chapter; no new account/model probe was performed.
- All three source figure PNGs and sample PDF pages containing Council data flow,
  equations and mission state transitions were visually inspected. Long diagrams
  are constrained to a printable page; tables wrap within printable width.

The rendering toolchain was installed outside the application: Mermaid CLI 11.17.0,
MathJax 3.2.1, Python Markdown 3.10.3, CairoSVG 2.9.1, Chromium/Puppeteer and native
font/browser libraries. PDFium was used only for PDF visual inspection. No runtime
dependency or application build artifact was changed. The first Mermaid render
identified a sequence-label semicolon parse error; its source was corrected, then
all twenty diagrams passed rendering.

```bash
/tmp/farmtact-docs-venv/bin/python docs/technical/figures/generate.py --png
/tmp/farmtact-docs-venv/bin/python docs/technical/render_diagrams.py \
  --mmdc /tmp/farmtact-docs-node/node_modules/.bin/mmdc \
  --puppeteer-config /tmp/farmtact-docs-puppeteer.json
/tmp/farmtact-docs-venv/bin/python docs/technical/render_report.py \
  --mathjax-module /tmp/farmtact-docs-node/node_modules/mathjax-full \
  --output reports/documentation/farmtact-technical-report.html
node docs/technical/export_pdf.cjs \
  /tmp/farmtact-docs-node/node_modules/puppeteer \
  reports/documentation/farmtact-technical-report.html \
  reports/documentation/farmtact-technical-report.pdf \
  reports/documentation/render-checks.json
python docs/technical/check_docs.py
git diff --check
```

## Disposition of findings

The README, documentation index, existing data/security/deployment/runbook guidance,
and both specifications now distinguish implemented mechanisms from future
requirements. Factual documentation errors are corrected. Runtime/game gaps are
recorded with source evidence and acceptance criteria for the next iteration; they
are not silently marked fixed. No new immutable application edition was published,
no Fly configuration was changed and no provider inference was requested.

Final printable report: **97 pages**.

Sprite checkpoint `v1` created after documentation verification. Runtime source
directories and release configuration are unchanged from the audit baseline.
