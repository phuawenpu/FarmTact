# App walkthrough and guide repair — completed 26 September 2026

Delivered: [5:21 master and chapters](../output/demo-video/README.md), three
narrated app guides, gateway routing repair and verified player fallbacks.
Evidence: [71 scoped checks](../reports/demo-video/README.md) plus build/contracts.
The files and source changes are committed; live edition publication is separate.

User scope: produce about five minutes of logical, real app interaction with natural
scrolling/typing, close-ups and English voiceover. Desktop and mobile run side by
side. Save the final video in `output/demo-video/` and push it there when below
100 MB. Repair the new-user demo media and commit each in-app guide below 100 MB.

Root owns gateway routing, numerical/app integration, recording, narration,
composition, media assets and verification. The bounded `demo_media_audit`
specialist owns only Explainers error states and its browser media audit. No
recursive delegation. Use isolated synthetic local farm sessions; real operations
stay disabled. Captures use the actual app and numerical backend, not invented
provider replies or result fixtures. Voice synthesis is local production tooling,
not an application inference provider.

The existing three 48-second text-slide MP4s are tracked, but the latest-only public
gateway omits `/explainers/`. Repair its explicit static directory allowlist and
verify MIME types, byte-range seeking, captions, poster and transcript requests.
Replace the slides with narrated app footage once the walkthrough is captured.
The four-scene introductory illustration remains distinct from the three videos.

Planned journey: introduction; inspect the dated order and records; calculate and
compare strategies; inspect Council roles and type a contextual question; review
changed assumptions; explicitly save a simulation plan; report a short result;
record an auditable correction; inspect recovery and the crop knowledge library.
Both viewport sessions must follow the same logical sequence. Synchronize narration
to scene markers, retaining a minimum five-minute captured interaction timeline.

Acceptance: inspect rendered frames and audio; verify total duration, actual
interaction logs, no unhandled page exceptions, seekable H.264/AAC MP4, captions,
file sizes and checksums. Exercise all introductory controls, media play/seek,
transcripts/downloads and injected failure states. Preserve publication boundaries:
repository changes do not overwrite the immutable deployed V22 source or image.
